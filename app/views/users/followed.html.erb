<h1>Follower User</h1>

<% if !@user.follower_user.blank? %> <!--ユーザーのフォロワーが存在しますか？を!をつけて存在しないになり、false→trueに分岐処理 -->
<table class="table">
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
    </tr>
  </thead>
  <tbody>
    <% @user.follower_user.each do |user| %>
  <tr>
      <td><%= attachment_image_tag user, :profile_image, :fill, 10, 10, fallback: "no-image-mini.jpg" %></td>
      <td><%= user.name %></td>
      <td>フォロー数：<%= user.follower.count %></td>
      <td>フォロワー数：<%= user.followed.count %></td>
      <td><%= link_to "Show", user_path(user) %></td>
  </tr>
    <% end %>
  </tbody>
</table>
<% else %> <!--条件分岐のtrueで存在しないになるのでelseを実行 -->
  <h4>ユーザーはいません</h4>
<% end %>